# Optional: public env vars (exposed to the browser) must be prefixed with NEXT_PUBLIC_
# NEXT_PUBLIC_APP_URL=https://example.com

# Server-only env vars (no prefix) are available only in API routes and server components
# API_SECRET=your-secret

# Sanity Studio (used when running sanity:dev / sanity:build from repo root)
# Copy this block to .env.local and set your project id from sanity.io/manage
# SANITY_PROJECT_ID=your-project-id
# SANITY_DATASET=production
# SANITY_API_VERSION=2024-01-01
# SANITY_READ_TOKEN=  (optional; only for private datasets)
